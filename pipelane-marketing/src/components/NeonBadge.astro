---
interface Props {
  text: string;
}

const { text } = Astro.props as Props;
---
<span class="chip neon text-xs uppercase tracking-[0.35em] text-[var(--color-primary)] font-semibold">
  {text}
</span>
