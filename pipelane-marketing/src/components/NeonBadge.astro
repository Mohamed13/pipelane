---
interface Props {
  text: string;
  class?: string;
}

const { text, class: extraClass = "" } = Astro.props as Props;
---
<span
  class={`chip neon text-xs uppercase tracking-[0.35em] font-semibold ${extraClass}`.trim()}
>
  {text}
</span>
