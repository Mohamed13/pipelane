---
import Icon from "./Icon.astro";

interface Props {
  metric: string;
  description: string;
  icon?: string;
}

const { metric, description, icon } = Astro.props as Props;
---
<div class="reveal glass rounded-3xl border border-line/30 p-6 text-left">
  <div class="flex items-start gap-3">
    {icon && (
      <span class="mt-1 text-primary">
        <Icon name={icon} size={26} />
      </span>
    )}
    <div>
      <p class="text-3xl font-semibold text-primary">{metric}</p>
      <p class="mt-3 text-sm leading-relaxed text-onSurfaceStrong">{description}</p>
    </div>
  </div>
</div>


