# Frontend environment variables (copy to .env for local dev)
# Defaults are used when unset.

# Base URL of the backend API
API_BASE_URL=http://localhost:56667

# Enable console demo controls
DEMO_MODE=false
